name: CI/CD Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
        node-version: ["22.x"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- 1. Python Environment Setup ---
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          # Explicitly set cache to empty string to prevent it from looking for requirements.txt
          cache: ''

      - name: Install Python dependencies / libraries
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      # --- 2. Node.js Environment Setup ---
      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install Node dependencies
        run: npm ci

      # --- 3. Linting & Testing ---
      - name: Run Python Syntax/Import Checks
        run: |
          # Verify that python modules compile without syntax errors
          python -m compileall ai/

      - name: Run TypeScript Tests (Vitest)
        run: npm run test:run